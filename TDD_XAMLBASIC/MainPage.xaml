<Page
    x:Class="TDD_XAMLBASIC.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TDD_XAMLBASIC"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="50"/>
        </Style>
    </Page.Resources>

    <Grid>
        <StackPanel>
            <Button Width="200" FontSize="50" 
                Foreground="White" Background="Red">Hello</Button>
            <Button Width="200" FontSize="50" 
                Foreground="White" Background="Green">World</Button>
            <Button Width="200" FontSize="50" 
                Foreground="White" Background="Blue">XAML</Button>
        </StackPanel>
    </Grid>

</Page>
