<Page
    x:Class="TDD_XAMLBASIC.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TDD_XAMLBASIC"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <Style x:Key="Large" TargetType="Button">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="50"/>
        </Style>
        <Style x:Key="Small" TargetType="Button">
            <Setter Value="100" Property="Width"/>
        </Style>
    </Page.Resources>
    <Grid>
        <StackPanel>
            <Button Style="{StaticResource Large}" 
                    Background="Red">Hello</Button>
            <Button Style="{StaticResource Large}" 
                    Background="Green">World</Button>

            <Button Style="{StaticResource Small}"
                    Background="Red">Hello</Button>
            <Button Style="{StaticResource Small}" 
                    Background="Green">World</Button>
        </StackPanel>
    </Grid>


</Page>
